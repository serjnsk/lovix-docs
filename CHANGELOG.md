# Ченджлог документации

Живёт только в репозитории: на Pages не публикуется, в навигацию не добавляется. Каждое содержательное изменение доки — строка тем же коммитом (см. DOCS_RULES.md).

## 2026-08-12

- нав: Раздеватор (дропдаун «Продукты ▾») и «UI нотификаций» убраны из меню по решению Сержа — репо только про Lovix, дизайн живёт в отдельном дизайн-проекте; файлы страниц остаются в репо.
- DOCS_RULES: раздел «Скоуп репо» (только Lovix; спека = бизнес-правила и архитектура, без дизайна и кода реализации), правило 1.3 сужено до превью контента, лимит нав возвращён к 8.

## 2026-08-10

- razdevator-ui: мобильная секция переработана в рабочий прототип — 4 таба (Фото/Видео/Баланс/Профиль), полный флоу генерации тапами (каталог → шторка → тост → блюр-карточка → готово), драг шторки с блокировкой при старте, свайп тоста, списание баланса, подтверждение удаления аккаунта.
- razdevator-ui: дизайн-система razdevator.hot по UI-STYLE лендинга — палитра с копированием, типографика, живые компоненты (кнопки, дропзона, топ-4 темплейта, до/после, тост, модалка с блокировкой, история с блюром, лоадер) с код-табами и блоками «почему так», мобильный phone-frame; в «Продукты ▾» теперь флоу + UI-кит.
- razdevator: запуск из каталога (К1–К4: модалка шаблона «до→после», цена на кнопке, блокировка до старта, тост) + решения — параллель без лимита, готовность только в истории, фейл с автовозвратом токенов без ретрая, вход email+Google.
- razdevator: SSR-лендинг + SPA (Т1–Т2), вход через «Войти», модалка объекта в истории (ещё раз / другое / удалить), правки мобильного лендинга (спойлер под H1, драг-н-дроп выше).
- razdevator: спека флоу razdevator.hot — лендинг → регвол (стартовые токены) → генерация/пейвол, страница генерации с блюром, история, сайдбар (Фото/Видео/Баланс/Профиль), критерии приёмки; в нав добавлен дропдаун «Продукты ▾», лимит верхних элементов в правилах — 9.
- support: тексты болванок убраны со страницы — шаблоны ответов только в HappyDesk (минимальный набор перечислен правилами).
- support: шаблоны живут в HappyDesk (проверить и актуализировать отключение подписки) + регулярка «понедельник — скрины репорта из дэшборда в AI Support».
- support: раздел «Каналы» (всё в HappyDesk: Lovix/Botfans/Milki — почта, Examka/Sdal — TG-боты) и EN-версии шаблонов с правилом «отвечаем на языке обращения».
- support: инструкция QA+Support — маршрутизатор обращений по пяти продуктам (чарджбеки, баги, функционал, отмены/возвраты, агрессия, сотрудничество), шаблоны ответов, чек-лист + пункт «Саппорт» в нав; лимит верхнего нав в правилах поднят до 8.
- email-builder: UI шаблонизатора перекрашен в светлую тему; превью письма остаётся тёмным (контент письма не тронут).
- locale-detection: страница переведена на светлую тему документации (синий акцент, светлые схема и симулятор).
- DOCS_RULES.md, CHANGELOG.md: введены правила документации (темы оформления, обязательные блоки спек, чек-лист структуры) и этот ченджлог с бэкфиллом истории.
- admin-demo: окно демо перекрашено в светлый UI реальной админки (крошки, underline-вкладки, цветной тулбар).
- admin-content: спека выровнена с демо — табы «Категории»/«Шаблоны», слаг и локали в КТ1, КТ11, критерии приёмки, обновлённая схема.
- admin-demo: раздел переименован в «Управление контентом», удалены статичные мокапы, добавлены «Что нужно сделать» и критерии приёмки.
- нав: «Админка ▾» стала дропдауном (спека + демо); в таблице категорий демо появились столбцы слага и локалей ru/en/de.
- admin-demo: drag-n-drop (pin на категорию, ручной порядок выдачи), тулбар генерации из «Страниц», флоу «Создать из фото» и «Генерация превью»; в спеку добавлены КТ8–КТ10, С5 и задачи Э1-5/Э1-6/Э2-5.
- admin-demo: живое демо раздела на двух табах — категории с галочками и pin/exclude, карточка бота с мультипривязкой и диагностикой характеристик.
- admin-content: спека модуля «Управление контентом» (pin/exclude поверх автофильтра, промпты per категория, метки фото, развод SEO/контента, декомпозиция) + пункт в нав.
- locale-detection: живая исполняемая спека автоопределения локали через CF — симулятор, схема с подсветкой пути, 9 прогоняемых критериев приёмки + пункт в нав.
- trigger-*: сборщик наследует дропдаун-нав из шаблона и помечает страницы собственным комментарием генератора.
- trigger-marketing: бутстрап-правила запуска по спящей базе (Б1–Б5): анонс волнами, winback от T0 без временных якорей, хвост для старых Free.

## 2026-08-09

- репо стало source of truth: md-мастера MODULE_1–4, шаблоны `_p*`, build.sh и README контрибьютора закоммичены (Серж).
- нав: двухуровневое меню — дропдаун «Рассылки ▾» на всех девяти страницах (Серж).
- партиалы шапок синхронизированы с нав-пунктами коллаборатора (Серж).

## 2026-08-07

- notifications-ui: мобильные паттерны — push-баннер со свайп-дизмиссом и bottom sheet с жестами.
- email-builder: кнопка очистки письма (пустой шаблон + сброс меты).
- notifications-ui: UI-кит нотификаций в прод-дизайне Lovix — тосты/колокольчик/стена, код-табы Blade/Vue/CSS/JS, живой Vue-плейграунд + пункт в нав.
- trigger-marketing: У7-1 получил email-крючок о непрочитанном и агрегатное письмо при нескольких компаньонах; правило П8 «отправитель — последний компаньон».
- trigger-*: тексты токенов приведены к решению токеномики (подписочные сгорают, купленные навсегда); решения треда зафиксированы.
- email-builder: Tiptap-редактор с живым превью и копированием HTML + пункт в нав (параллельная сессия).
- email-template: спека email-шаблона и письмо-пример «start guide» + пункт в нав (параллельная сессия).
- trigger-*: mermaid-схемы на Miro-холсте (пан/зум/пинч), клики по узлам с панелью деталей; страницы дезслопифицированы в плотные таблицы текстов.
- trigger-system, trigger-marketing: контентные спеки триггерных рассылок (тексты, каналы, правила, решения) поверх «Сценариев» + пункты в нав.

## 2026-08-05

- scenarios: документ «Сценарии взаимодействия» — 27 триггеров в 6 группах, правила доставки, квоты по каналам, карта сценариев, категории отписки; серия уточнений (проактив от компаньона, продления без напоминаний при автосписании, слияние У1-4/У1-5, «первый занял слот») (Серж).
- events: канонический реестр событий с потребителями (метрики/сценарии) (Серж).
- metrics: система метрик — 8 групп, 40+ паспортов, воронки Г3/Г4 с визуалом; серия правок структуры и чипов (Серж).
- все доки: сайдбар-TOC с якорями и подсветкой скролла (Серж).

## 2026-08-04

- index: финальная платёжная схема — локаль определяет тексты и валюту отображения, способ оплаты определяет валюту списания (Серж).

## 2026-08-03

- index: Free-тариф как 5 сообщений/мес + разовый настраиваемый пробный набор; апгрейд с зачётом остатка; решение «подписочные токены сгорают ежемесячно, купленные — никогда»; правила локали/валюты (Серж).

## 2026-08-02

- index: первый документ токеномики — тарифы, пакеты, прайс операций, прототипы; белая тема, лимиты «безлимит со скрытым капом» (Серж).
